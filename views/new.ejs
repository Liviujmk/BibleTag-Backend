<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>New article</title>
    <link rel="stylesheet" href="/public/style.css">
</head>
<body>
    <%- include('layouts/header') %>
    <div class="main-container">
        <div class="mcn">
            <h2>Create a new article</h2>
            <form action="/articles" method="post">
                <input type="text" name="title" placeholder="Title" value="<%= article.title %>">
                <br>
                <input type="text" name="collectionName" placeholder="Collection" value="<%= article.collectionName %>">
                <br>
                <textarea name="markdown" placeholder="Content" value="<%= article.markdown %>" style="height: 100px;"></textarea>
                <br>
                <button type="submit" class="button default">Create</button>
            </form>
        </div>
    </div>
    <script>
        let queryParam = new URLSearchParams(window.location.search);
        let collectionName = queryParam.get('collectionName');
        console.log(collectionName)
        if (collectionName) {
            document.querySelector('input[name="collectionName"]').value = collectionName;
        }
        
    </script>
</body>
</html>